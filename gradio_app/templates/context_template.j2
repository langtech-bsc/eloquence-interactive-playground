The following are excerpts retrieved from the indexed documents.
Use the relevant excerpts to answer the user's question and ignore excerpts that are unrelated.
Base the answer on the provided context and do not fill in missing information with assumptions.
If the context is insufficient, say that the answer cannot be determined from the available documents.

If you are using fact derived from a document, provide a number given as "Doc no. x" as a reference.
Include the number in brackets after the passage, in format "[doc x]"
For example:
The capital of Czechia is Prague [doc 2].
Use the citations immediately as the information appears in the text.

Context documents:
{% for doc in documents %}
---
Doc no. {{loop.index}}
{{ doc }}

{% endfor %}
